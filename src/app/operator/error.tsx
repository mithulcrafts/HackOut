"use client";
export default function OperatorError({ reset }: { reset: () => void }) { return <main className="shell"><div className="container" style={{ paddingTop: 48 }}><div className="card" role="alert"><div className="eyebrow">Operator view unavailable</div><h1>We could not load the current scenario.</h1><p className="muted">The scenario may have been reset. Try loading it again.</p><button className="status" onClick={() => reset()}>Try again</button></div></div></main>; }
